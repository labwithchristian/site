# Hugo
/public/
/resources/_gen/
.hugo_build.lock
hugo.exe
hugo.darwin
hugo.linux

# Node (not needed for a normal build; fonts are already vendored in static/fonts/)
node_modules/

# OS
.DS_Store

# Local exports from the Claude desktop app, never site content
Claude outputs/

# Resume PDFs: tailored per role and never published from this repo
content/resume/*.pdf
