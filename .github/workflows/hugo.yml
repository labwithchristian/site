name: Build, check and deploy

# Pull requests: build the site and check every internal link, no deploy.
# Pushes to main and the daily schedule: the same checks, then deploy to GitHub Pages.
on:
  push:
    branches: ["main"]
  pull_request:
  workflow_dispatch:
  # Daily rebuild so date-driven content stays current without a commit: the
  # /ctf event states, the resume's "Competing next" line and the homepage's
  # What's Cooking row are all worked out from data/ctf.yaml at build time.
  # 13:15 UTC is 9:15 am US Eastern in daylight time (8:15 am in winter), just
  # after events that open at 9 am Eastern, such as Huntress CTF 2026.
  # GitHub pauses scheduled runs after 60 days without a push; any commit (or
  # "Run workflow") turns them back on.
  schedule:
    - cron: "15 13 * * *"

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.166.0
      SITE_URL: https://christiancarrasco.dev
    steps:
      - name: Install Hugo CLI
        run: |
          curl -sL "https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb" -o /tmp/hugo.deb \
          && sudo dpkg -i /tmp/hugo.deb

      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Build with Hugo
        run: |
          hugo --gc --minify --baseURL "${SITE_URL}/"

      # Offline: internal pages, anchors, CSS, JS and images must exist in the
      # build. Absolute links to the site itself are mapped back onto ./public.
      # External links are not fetched, so a slow third-party site can't fail
      # a deploy.
      - name: Check internal links
        uses: lycheeverse/lychee-action@v2
        with:
          fail: true
          args: >-
            --offline
            --no-progress
            --include-fragments
            --root-dir ${{ github.workspace }}/public
            --index-files index.html
            --remap 'https://christiancarrasco\.dev/(.*) file://${{ github.workspace }}/public/$1'
            './public/**/*.html'

      # v3 keeps dot folders, so /.well-known/security.txt deploys. From v4 on,
      # hidden files are left out: on v5+ add `include-hidden-files: true`.
      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    concurrency:
      group: pages
      cancel-in-progress: false
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
